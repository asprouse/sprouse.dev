# Claude Code project instructions

## Before committing

Run `npm run check` and fix everything it flags. Do not commit on red — fix the root cause, don't skip the hook or the check. A failing local check will fail CI too.
