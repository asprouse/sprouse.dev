## Summary

<!-- One or two sentences on what this PR changes and why. -->

## Test plan

<!-- How did you verify this? Include `npm run check` output if you touched code. -->

- [ ] `npm run check` passes locally
- [ ] If touching `/api/*` — manually tested with curl or the running app
- [ ] If touching `/cv` — confirmed print preview still looks right
- [ ] If touching `resume.json` — `npm run validate:resume` is clean
